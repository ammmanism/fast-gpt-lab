[project]
name = "fast-gpt-lab"
version = "1.1.0"
description = "High-performance, elite GPT implementation from first principles to Triton kernels."
authors = [{ name = "Elite Engineer", email = "engineer@example.com" }]
dependencies = [
    "torch>=2.2.0",
    "numpy>=1.26.0",
    "tqdm",
    "tiktoken",
]
readme = "README.md"
requires-python = ">=3.10,<3.14"

[project.optional-dependencies]
gpu = [
    "triton>=2.1.0",
    "flash-attn>=2.5.0",
    "bitsandbytes",
]
full = [
    "transformers",
    "wandb",
    "datasets",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.3.0",
    "mypy>=1.9.0",
    "pre-commit",
    "pandas",
    "matplotlib",
    "fastapi",
    "uvicorn",
    "aiohttp",
    "rich",
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "C90", "B"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["eval"]
python_files = "test_*.py"
